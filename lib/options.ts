import { type PluginOptions } from './types'

export const defaultOptions: Required<PluginOptions> = {
  colorScheme: 'content',
  mainColor: '#0CA678',
  fontFamily: {
    body: '',
    heading: ''
  }
}
